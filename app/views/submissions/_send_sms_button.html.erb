<div class="mt-2 mb-1">
  <%= button_to submitter.sent_at? ? t('re_send_sms') : t('send_sms'),
        submitter_send_sms_index_path(submitter_slug: submitter.slug),
        class: 'btn btn-sm btn-primary w-full',
        data: { turbo_confirm: submitter.sent_at? ? t('are_you_sure_') : nil } %>
</div>
